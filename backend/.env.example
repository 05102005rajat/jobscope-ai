DATABASE_URL=postgresql://user:password@host:5432/jobscope
GROQ_API_KEY=gsk-your-key-here
# Optional: if set, every /api/* route requires this value in an X-API-Key header.
# Leave unset for local dev to keep the API open.
API_KEY=
